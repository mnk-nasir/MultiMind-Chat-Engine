# Example environment variables

# Optional: OpenRouter API key (preferred for multi-vendor workflows)
OPENROUTER_API_KEY=

# Optional: OpenAI API key (used when agent model is 'openai/...')
OPENAI_API_KEY=

# Where outputs are written if using --save
OUTPUT_DIR=outputs

# Where session memory files are stored
MEMORY_DIR=memory
